<h1>Livros do Banco de dados</h1>
<div class="books-container">
    <a href="/">Adicionar Livros</a>
</div>
<div class="books-container">
    {{#each books}}
    <div>#{{this.id}} - <a href="/books/{{this.id}}"><strong>{{this.title}}</strong></a> - Pag: {{this.pageqty}}</div>
    {{/each}}
</div>